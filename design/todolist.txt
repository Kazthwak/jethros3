Memory allocator
A heap allocator that works, and a variable-length array system


Change disc format to use a single FAT32 drive that is booted from, rather than having 2 drives that the os needs to have in.
proper keyboard scancodes
filesystem support
program loading
system calls
usermode
security
kernel is un-pre-emptable
Check what the filesystem is
